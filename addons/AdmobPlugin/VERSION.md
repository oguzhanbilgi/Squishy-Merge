# AdmobPlugin — pinned release + Squishy Merge patch (M8.9-01 → M9-01)

Third-party Godot Android plugin: upstream **v6.0** plus ONE small, reviewed
source patch (M9-01). Everything else is the upstream release as built by the
upstream build scripts.

| | |
|---|---|
| Project | https://github.com/godot-sdk-integrations/godot-admob |
| Release | **v6.0** — "Admob Plugin v6.0", published 2026-02-01 |
| Tag commit | `90e3c616ea3c680e3875c31e6bcccffbebbe9d3b` ("Upgraded to Godot 4.6 (#89)") |
| Original asset | `AdmobPlugin-Android-v6.0.zip` (256 591 B), SHA-256 `9ec2600238f53104fcf9f21b9e8dec1525880af0d40f7a66878022aa05973f24` |
| Original AARs (on main up to `5a3a0f0`) | debug `388243802894363133814f9f8c0c9be61adc27b5911c11460483768700f6be63`, release `526516f93b1e29a6749b0293d86649bb7a6971603258a62e109dfd65afb36789` |
| **Patch (M9-01)** | `tools/admob_plugin/0001-ump-privacy-options-and-debug-geography.patch`, SHA-256 `57580a948b7c969ea83d5f0601d472c940f14c1f96967ca3af4c89f2a8b9c5eb` |
| **Patched `bin/debug/AdmobPlugin-debug.aar`** | SHA-256 `e3ac9a6b1492468928c037d4d21464310560c7b21c14c597fa4a567c23c6eb2d` |
| **Patched `bin/release/AdmobPlugin-release.aar`** | SHA-256 `90d359921f10bc6618ed63b9ea97cdba5afcbdfbb8cb72fe264834e5bf478284` |
| Patched `Admob.gd` | generated by the upstream `:addon:generateGDScript` task from the patched template (LF) |
| Licence | MIT (see `LICENSE` next to this file); the patch is under the same licence |
| Built against | `godot-lib` from the project's Godot **4.6.3** Android build template (upstream used 4.6.stable; the unpatched rebuild is class-for-class identical) |
| Google Mobile Ads SDK | `com.google.android.gms:play-services-ads:24.9.0` (added at export by `AdmobPlugin.gd` → `ANDROID_DEPENDENCIES`) — UNCHANGED |
| UMP SDK | `com.google.android.ump:user-messaging-platform:3.2.0` (transitive via `play-services-ads-api:24.9.0`) — UNCHANGED |
| Android | AAR `minSdkVersion 24`; plugin `compileSdk 35` (build-tools 36.1.0 instead of upstream 35.0.0 — no effect on the classes) |
| Plugin API | Godot Android plugin **v2** (`org.godotengine.plugin.v2.AdmobPlugin` meta-data) |

## What the patch changes (3 files, +73 / −2)

1. `ConsentConfiguration.java` — `debug_geography` accepts any `java.lang.Number`
   (Godot passes GDScript ints as `Long`; v6.0 only accepted `Integer` and
   silently ignored the value — upstream issue #120, still open; fixed in
   upstream v7.0, which targets Godot 4.7).
2. `AdmobPlugin.java` — three UMP 3.x calls exposed to Godot:
   `can_request_ads()` → `ConsentInformation.canRequestAds()`,
   `get_privacy_options_requirement_status()` →
   `ConsentInformation.getPrivacyOptionsRequirementStatus()` (`"REQUIRED"` /
   `"NOT_REQUIRED"` / `"UNKNOWN"`), `show_privacy_options_form()` →
   `UserMessagingPlatform.showPrivacyOptionsForm()` + new signal
   `privacy_options_form_dismissed(Dictionary)`. Not present upstream in v6.0,
   v7.0 or `main` (checked 2026-09-22).
3. `Admob.gd` (GDScript facade template) — the same three methods plus
   `has_privacy_options_api()` (true only when the native plugin registered the
   new signal, i.e. the patched AAR is installed) and the forwarded signal.

No other class, resource, dependency or version changed (the rebuild differs
from the unpatched build only in `AdmobPlugin*.class` — line numbers shift in
the anonymous inner classes — and `ConsentConfiguration.class`).

## Known defect (TASK/040, Samsung A36, 2026-09-25) — release gate CODE blocker

The committed AARs never apply `RequestConfiguration`: Godot 4.6 passes the facade's
dictionary ints as `java.lang.Long` and arrays as `Object[]`, while v6.0
`AdmobConfiguration` reads them with `(int)` / `(String[])` casts, so
`createRequestConfiguration()` throws `ClassCastException` (swallowed by Godot) and
`MobileAds.setRequestConfiguration()` is never reached — max ad content rating **G**,
TFCD / TFUA and test device ids are NOT in effect. Fixed in the feasibility-only
`tools/admob_plugin/0002-…patch` (Number / Object[]-safe reads); these production
binaries are unchanged. `tools/release/release_readiness.gd` `KNOWN_PLUGIN_DEFECTS`
reports it as CODE until a fixed build replaces this release AAR.

## Rebuild / verify

`tools/admob_plugin/README.md`. In short, from the repo root:

```
tools/admob_plugin/build_patched_plugin.sh baseline   # unpatched v6.0 == upstream release AARs
tools/admob_plugin/build_patched_plugin.sh verify     # committed files == v6.0 + patch
```

Why v6.0 and not v7.0 (2026-05-27): v7.0 is built against **Godot 4.7 beta1**
(`godot.properties`: `godotVersion=4.7`, `godotReleaseType=beta1`) and the
maintainer states in issue #122 that "v7.0 requires Godot 4.7+ … for Godot 4.6.x
use v6.0". This project is pinned to Godot **4.6.3** (CLAUDE.md). v7.0 also does
NOT expose the three privacy-options calls — only the #120 fix.

Google SDK 24.x support window (developers.google.com/admob/android/deprecation,
read 2026-09-21): *Supported*; deprecation 2027-06-30, sunset 2028-06-30.

Upgrading: do NOT copy a newer release zip over this folder — that silently
drops the patch. Re-base the patch on the new upstream tag, rebuild with the
script, update this file, keep `android_export.cfg` (project configuration,
not part of the release). Full notes: `docs/monetization/ADS_SYSTEM.md`,
`docs/monetization/PRIVACY_CONSENT.md`.
