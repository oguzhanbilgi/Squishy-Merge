diff --git a/addon/src/Admob.gd b/addon/src/Admob.gd
index 7b70508..30eea5f 100644
--- a/addon/src/Admob.gd
+++ b/addon/src/Admob.gd
@@ -59,6 +59,7 @@ signal consent_form_dismissed(error_data: FormError)
 signal consent_form_failed_to_load(error_data: FormError)
 signal consent_info_updated
 signal consent_info_update_failed(error_data: FormError)
+signal privacy_options_form_dismissed(error_data: FormError)
 signal tracking_authorization_granted
 signal tracking_authorization_denied
 
@@ -470,6 +471,8 @@ func _connect_signals() -> void:
 	_plugin_singleton.connect("consent_form_failed_to_load", _on_consent_form_failed_to_load)
 	_plugin_singleton.connect("consent_info_updated", _on_consent_info_updated)
 	_plugin_singleton.connect("consent_info_update_failed", _on_consent_info_update_failed)
+	if _plugin_singleton.has_signal("privacy_options_form_dismissed"):
+		_plugin_singleton.connect("privacy_options_form_dismissed", _on_privacy_options_form_dismissed)
 	if _plugin_singleton.has_signal("tracking_authorization_granted"):
 		_plugin_singleton.connect("tracking_authorization_granted", _on_tracking_authorization_granted)
 	if _plugin_singleton.has_signal("tracking_authorization_denied"):
@@ -1123,6 +1126,37 @@ func is_consent_form_available() -> bool:
 	return false
 
 
+## True when the native plugin exposes the UMP privacy-options API below
+## (can_request_ads / get_privacy_options_requirement_status / show_privacy_options_form).
+func has_privacy_options_api() -> bool:
+	return _plugin_singleton != null and _plugin_singleton.has_signal("privacy_options_form_dismissed")
+
+
+## UMP ConsentInformation.canRequestAds().
+func can_request_ads() -> bool:
+	if not has_privacy_options_api():
+		Admob.log_error("%s: can_request_ads() is not available" % PLUGIN_SINGLETON_NAME)
+		return false
+	return _plugin_singleton.can_request_ads()
+
+
+## UMP ConsentInformation.getPrivacyOptionsRequirementStatus():
+## "REQUIRED", "NOT_REQUIRED" or "UNKNOWN".
+func get_privacy_options_requirement_status() -> String:
+	if not has_privacy_options_api():
+		Admob.log_error("%s: get_privacy_options_requirement_status() is not available" % PLUGIN_SINGLETON_NAME)
+		return "UNKNOWN"
+	return _plugin_singleton.get_privacy_options_requirement_status()
+
+
+## UMP UserMessagingPlatform.showPrivacyOptionsForm(); emits privacy_options_form_dismissed.
+func show_privacy_options_form() -> void:
+	if not has_privacy_options_api():
+		Admob.log_error("%s: show_privacy_options_form() is not available" % PLUGIN_SINGLETON_NAME)
+		return
+	_plugin_singleton.show_privacy_options_form()
+
+
 func update_consent_info(a_parameters: ConsentRequestParameters = null) -> void:
 	if _plugin_singleton == null:
 		Admob.log_error("%s plugin not initialized" % PLUGIN_SINGLETON_NAME)
@@ -1420,6 +1454,10 @@ func _on_consent_info_update_failed(error_data: Dictionary) -> void:
 	consent_info_update_failed.emit(FormError.new(error_data))
 
 
+func _on_privacy_options_form_dismissed(error_data: Dictionary) -> void:
+	privacy_options_form_dismissed.emit(FormError.new(error_data))
+
+
 func _on_tracking_authorization_granted() -> void:
 	tracking_authorization_granted.emit()
 
diff --git a/android/src/main/java/org/godotengine/plugin/admob/AdmobPlugin.java b/android/src/main/java/org/godotengine/plugin/admob/AdmobPlugin.java
index 30460fa..2a47015 100644
--- a/android/src/main/java/org/godotengine/plugin/admob/AdmobPlugin.java
+++ b/android/src/main/java/org/godotengine/plugin/admob/AdmobPlugin.java
@@ -114,6 +114,7 @@ public class AdmobPlugin extends GodotPlugin {
 	static final String SIGNAL_CONSENT_FORM_DISMISSED = "consent_form_dismissed";
 	static final String SIGNAL_CONSENT_INFO_UPDATED = "consent_info_updated";
 	static final String SIGNAL_CONSENT_INFO_UPDATE_FAILED = "consent_info_update_failed";
+	static final String SIGNAL_PRIVACY_OPTIONS_FORM_DISMISSED = "privacy_options_form_dismissed";
 
 	private static final String PREFS_NAME = "godot_admob_settings";
 	private static final String PREF_KEY_AD_VOLUME = "ad_volume";
@@ -242,6 +243,7 @@ public class AdmobPlugin extends GodotPlugin {
 
 		signals.add(new SignalInfo(SIGNAL_CONSENT_INFO_UPDATED));
 		signals.add(new SignalInfo(SIGNAL_CONSENT_INFO_UPDATE_FAILED, Dictionary.class));
+		signals.add(new SignalInfo(SIGNAL_PRIVACY_OPTIONS_FORM_DISMISSED, Dictionary.class));
 
 		return signals;
 	}
@@ -980,6 +982,36 @@ public class AdmobPlugin extends GodotPlugin {
 		return UserMessagingPlatform.getConsentInformation(activity).isConsentFormAvailable();
 	}
 
+	@UsedByGodot
+	public boolean can_request_ads() {
+		boolean canRequestAds = UserMessagingPlatform.getConsentInformation(activity).canRequestAds();
+		Log.d(LOG_TAG, String.format("can_request_ads(): %s", canRequestAds));
+		return canRequestAds;
+	}
+
+	@UsedByGodot
+	public String get_privacy_options_requirement_status() {
+		ConsentInformation.PrivacyOptionsRequirementStatus status =
+				UserMessagingPlatform.getConsentInformation(activity).getPrivacyOptionsRequirementStatus();
+		Log.d(LOG_TAG, String.format("get_privacy_options_requirement_status(): %s", status));
+		if (status == ConsentInformation.PrivacyOptionsRequirementStatus.REQUIRED) {
+			return "REQUIRED";
+		} else if (status == ConsentInformation.PrivacyOptionsRequirementStatus.NOT_REQUIRED) {
+			return "NOT_REQUIRED";
+		}
+		return "UNKNOWN";
+	}
+
+	@UsedByGodot
+	public void show_privacy_options_form() {
+		Log.d(LOG_TAG, "show_privacy_options_form()");
+		activity.runOnUiThread(() -> {
+			UserMessagingPlatform.showPrivacyOptionsForm(activity, (ConsentForm.OnConsentFormDismissedListener) formError -> {
+				emitSignal(SIGNAL_PRIVACY_OPTIONS_FORM_DISMISSED, GodotConverter.convert(formError));
+			});
+		});
+	}
+
 	@UsedByGodot
 	public void update_consent_info(Dictionary consentRequestParameters) {
 		ConsentConfiguration consentConfig = new ConsentConfiguration(consentRequestParameters);
diff --git a/android/src/main/java/org/godotengine/plugin/admob/model/ConsentConfiguration.java b/android/src/main/java/org/godotengine/plugin/admob/model/ConsentConfiguration.java
index 4b90f82..b4a777d 100644
--- a/android/src/main/java/org/godotengine/plugin/admob/model/ConsentConfiguration.java
+++ b/android/src/main/java/org/godotengine/plugin/admob/model/ConsentConfiguration.java
@@ -55,8 +55,9 @@ public class ConsentConfiguration {
 
 			if (_data.containsKey(DEBUG_GEOGRAPHY_PROPERTY)) {
 				Object debugGeographyObj = _data.get(DEBUG_GEOGRAPHY_PROPERTY);
-				if (debugGeographyObj instanceof Integer) {
-					int debugGeography = (int) debugGeographyObj;
+				// Godot passes GDScript ints as java.lang.Long, not Integer (upstream #120).
+				if (debugGeographyObj instanceof Number) {
+					int debugGeography = ((Number) debugGeographyObj).intValue();
 					Log.d(LOG_TAG, "Setting debug geography to: " + debugGeography);
 					debugSettingsBuilder.setDebugGeography(debugGeography);
 				} else {
